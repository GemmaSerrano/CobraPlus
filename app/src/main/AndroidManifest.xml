<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_cobraplus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_cobraplus_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CobraPlus">

        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.MainActivityAdmin"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.Profile"/>
        <activity android:name=".activities.ManagementConcepts"/>
        <activity android:name=".activities.NewConcept"/>
        <activity android:name=".activities.NewConceptAdmin"/>
        <activity android:name=".activities.RecordActivity"/>
        <activity android:name=".activities.ManagementReports"/>
        <activity android:name=".activities.NewReport"/>
        <activity android:name=".activities.NewReportAdmin"/>
        <activity android:name=".activities.SeeReport"/>
        <activity android:name=".activities.ConfirmReport"/>
        <activity android:name=".activities.EditRecord"/>
        <activity android:name=".activities.HelpActivity"/>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>